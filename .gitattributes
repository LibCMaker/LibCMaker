## Init LFS, you only need to run this once per user account:
# git lfs install

## To change the file:
# git lfs track "cmake/lfs/*.tar.*"

## Then:
# git add .gitattributes
# git commit ...

cmake/lfs/*.tar.* filter=lfs diff=lfs merge=lfs -text
